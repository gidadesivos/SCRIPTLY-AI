# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Skills instaladas por `npx skills add` — ferramental local do agente, não
# código do produto. O .mcp.json fica versionado (foi criado com --scope
# project, que significa exatamente isso); estas 212K de markdown de terceiro,
# não. Reverter: tirar estas linhas e `git add .agents .claude skills-lock.json`.
.agents/
.claude/
skills-lock.json
